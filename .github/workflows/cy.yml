name: Test CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      JWT_SECRET: postgresql://user:password@localhost:5432/api-db?schema=public

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.x

      - name: Use MongoDB
        run: ./scripts/start-db.sh

      - name: Install dependencies
        run: npm i

      - name: Run tests
        run: npm run test
